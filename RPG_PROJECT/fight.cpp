#include"bag.h"

void fight_function(int character_blood, int character_attack, int character_type, string monster_name, int monster_blood, int monster_attack){
	srand(time(NULL));
	if (rand() % 2) {
		cout << "你取得了先攻" << endl;
	}
}